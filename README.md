# nas-protocol

This project is building upon TCP 

BASIC STRUCTURE:
    each file transfer will have its size in the headder 
    While for list all command it will have number of files in the headder (still working on this one)